--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# FastAPI & Server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
alembic>=1.13.0

# Data & Analysis
yfinance>=0.2.40
pandas>=2.0.0
# numpy 2.2.6 is the EXACT sweet spot:
# - torch 2.5.1+cpu: needs numpy 2.x ✅
# - opencv 4.13: needs numpy>=2.0 ✅
# - pandas-ta: needs numpy>=2.2.6 ✅
# - numba 0.61.2: needs numpy<=2.2 ✅
numpy==2.2.6
pandas-ta>=0.4.71b0

# Configuration
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Scheduling
apscheduler>=3.10.0

# HTTP & Async
httpx>=0.27.0
aiohttp>=3.9.0

# Utilities
loguru>=0.7.0
pytz>=2024.1
tenacity>=8.3.0

# WebSocket support
websockets>=12.0

# ── Phase 2: Pattern Recognition ──────────────────────────────────────────────
# Chart generation
mplfinance>=0.12.10b0
matplotlib>=3.8.0
Pillow>=10.0.0

# Signal processing & ML
scipy>=1.13.0
scikit-learn>=1.5.0
xgboost>=2.0.0
joblib>=1.4.0

# Computer Vision
opencv-python-headless>=4.9.0

# Gemini Vision API
google-generativeai>=0.7.0

# Data storage
pyarrow>=16.0.0

# Expert Model (PyTorch)
# torch 2.5.1+cpu: supports numpy 2.x AND fixes fbgemm.dll WinError 126 on Windows.
# Do NOT downgrade to 2.4.0 (fbgemm.dll broken) or 2.3.1 (numpy 1.x only).
torch==2.5.1+cpu
torchvision==0.20.1+cpu
timm>=0.9.0

# Broker API Integration
fyers-apiv3>=3.1.2
pyotp>=2.9.0

# Google Cloud
google-cloud-aiplatform>=1.50.0

# Options Greeks
py_vollib

# Kronos AI
einops
transformers
